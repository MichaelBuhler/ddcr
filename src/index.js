
const {
  getConsolidatedFileChangesBetweenDirectories
} = require('./Differ')

const HtmlRenderer = require('./HtmlRenderer')

module.exports = {
  getConsolidatedFileChangesBetweenDirectories,
  HtmlRenderer
}
